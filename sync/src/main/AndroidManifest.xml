<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.unclealex.sync.devicesynchroniser.sync" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="uk.co.unclealex.sync.devicesynchroniser.sync.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="uk.co.unclealex.sync.devicesynchroniser.sync.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="uk.co.unclealex.sync.devicesynchroniser.sync.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.unclealex.sync.devicesynchroniser.sync.MainActivity" />
        </activity>

        <service
            android:name="uk.co.unclealex.sync.devicesynchroniser.sync.SynchroniseService"
            android:exported="false" >
        </service>
    </application>

</manifest>
